# Open TV Provider-Vault Proof — 2026-05-26

Status: `provider-ready`

## What changed

- Added a tracked `static-web-overrides/` layer for the hosted Angular build.
- Added `hls.js` and a browser player overlay so the hosted app no longer calls
  native Tauri/mpv playback.
- Apollo Group TV and XtremeHD now load from DaveTV provider-vault catalogs.
- Live channels and movies use safe same-origin `/api/provider-vault/stream`
  URLs.
- The hosted build skips the old "add your first source" Tauri flow when vault
  providers are configured.
- HLS playback uses larger buffers:
  `maxBufferLength=180`, `maxMaxBufferLength=600`, `backBufferLength=90`.

## Verification

- `npx ng build` passed from `G:\Github\IPTV-web\open-tv`.
- Playwright visual/provider proof passed:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\open-tv-provider-vault-proof-20260526\summary.json`
- Screenshot:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\open-tv-provider-vault-proof-20260526\open-tv-provider-vault-live.png`

Assertions from the proof:

- Apollo Group TV and XtremeHD are both configured.
- Provider catalogs load from `/api/provider-vault/catalog`.
- English UI chrome is visible.
- Apollo and XtremeHD sample channels are visible.
- The native setup screen is not shown.
- Stream URLs use `/api/provider-vault/stream`.
- No raw credential-shaped URL is rendered or requested.
- HLS buffer settings are bundled.
- Zero Tauri invoke errors, console errors, or page errors.

## VPS deployment

- Deployed static build to `/var/www/davetv/apps/open-tv`.
- VPS `index.html` sha256:
  `ca6095b6a6b3a39a40a763796ef84a2125f31d94dc3a714f7402c77319623753`
- Backup:
  `/var/backups/daveai-apps/open-tv-before-provider-ready-20260526T190851Z.tar.gz`
- Launcher state:
  `provider-ready`, `Apollo + XtremeHD via DaveTV provider vault`.
- External route:
  `https://apps.daveai.tech/open-tv/` returns the expected auth-gate redirect
  to `/login?return_to=...`.
