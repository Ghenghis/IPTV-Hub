# IPTVnator Provider Vault Proof - 2026-05-27

## Status

`PASS` on live `https://iptvnator.daveai.tech/`.

## Fix

- Bumped the DaveAI bootstrap from `20260526-v5` to `20260527-v6`.
- Added a stale-Xtream route watchdog so old `/workspace/xtreams/...`
  workspaces are repaired into `/workspace/playlists/daveai-provider-vault-*/all`
  even if cached Angular state tries to render `Portal unavailable`.
- Preserved English defaults, DaveAI vault playlist rows, and server-side-only
  provider credentials.

## Live E2E Proof

Main provider proof:

```text
C:\Users\Admin\Downloads\VPS\_visual_artifacts\iptvnator-provider-proof-20260527
```

Exact user failure-route proof:

```text
C:\Users\Admin\Downloads\VPS\_visual_artifacts\iptvnator-exact-route-proof-20260527
```

Verifier results:

- the screenshot route
  `/workspace/xtreams/0c911b96-4d88-45f4-bcf9-c71586cf0428/vod`
  redirects to `/workspace/playlists/daveai-provider-vault-xtremehd/all`;
- `BUILD_ID` and seed marker are `20260527-v6`;
- route repair marker is `20260527-v6:install`;
- IndexedDB contains both DaveAI vault playlists with `2600` rows each;
- XtremeHD playback reached `video.readyState=4`;
- Apollo Group TV playback reached `video.readyState=4`;
- two same-origin `/api/provider-vault/stream` responses returned `200`;
- no `Portal unavailable` screen;
- no page errors or console errors;
- no credential-shaped text was visible in the browser.

## Live Backup

Pre-deploy backup:

```text
/var/backups/daveai-apps/iptvnator-before-v6-20260527T034252Z.tgz
```
