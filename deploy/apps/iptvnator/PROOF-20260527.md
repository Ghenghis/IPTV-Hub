# IPTVnator Provider Vault Proof - 2026-05-27

## Status

`PASS` on live `https://iptvnator.daveai.tech/`.

## Fix

- Bumped the DaveAI bootstrap to `20260527-v8`.
- Replaced the old boolean seed check with a real playlist-health check:
  provider id, DaveAI vault source, current build id, non-zero `count`, and
  non-empty `playlist.items` are all required before setup is considered done.
- Kept the stale Xtream route watchdog so old
  `/workspace/xtreams/...` URLs repair into
  `/workspace/playlists/daveai-provider-vault-*/all`.
- Preserved English defaults and server-side-only provider credentials.

## Live E2E Proof

Main provider proof:

```text
C:\Users\Admin\Downloads\VPS\_visual_artifacts\iptvnator-provider-proof-20260527
```

Verifier results:

- the screenshot route
  `/workspace/xtreams/0c911b96-4d88-45f4-bcf9-c71586cf0428/vod`
  redirects to `/workspace/playlists/daveai-provider-vault-xtremehd/all`;
- proof deliberately poisoned the browser first with current-looking but empty
  DaveAI vault rows, then verified the app repaired them;
- `BUILD_ID` and seed marker are `20260527-v8`;
- IndexedDB contains both DaveAI vault playlists with `2600` rows each;
- XtremeHD playback reached `video.readyState=4`;
- Apollo Group TV playback reached `video.readyState=4`;
- two same-origin `/api/provider-vault/stream` responses returned `200`;
- no `Portal unavailable` screen;
- no page errors or console errors;
- no credential-shaped text was visible in the browser.

## Live Backup

Pre-deploy backup:

```text
/var/backups/daveai-apps/iptvnator-before-v8-20260527T190541Z.tgz
```
