# Stalker UI Provider-Vault Proof — 2026-05-26

Status: `provider-ready`

## What changed

- Added a tracked `static-web-overrides/` layer for the live React/Vite
  Stalker UI build used by `apps.daveai.tech/stalker-ui/`.
- Added Apollo Group TV and XtremeHD provider-vault catalog loading for movies,
  series, and live TV.
- Playback URLs now pass through safe same-origin
  `/api/provider-vault/stream` URLs instead of requiring raw provider data.
- Hosted API calls default to same-origin `/api`.
- Live TV now opens on `All Channels` by default so provider channels are
  immediately visible.
- Provider-vault mode returns an empty EPG payload locally instead of hitting
  stale legacy `/api/v2/epg` and producing console errors.

## Verification

- `npm run build` passed from `G:\Github\IPTV-web\stalker-ui`.
- Playwright visual proof passed with stubbed provider-vault endpoints:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\stalker-ui-provider-vault-proof-20260526\summary.json`
- Screenshot:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\stalker-ui-provider-vault-proof-20260526\stalker-ui-provider-vault-live.png`

Assertions from the proof:

- Apollo Group TV and XtremeHD are both configured.
- Provider catalogs load from `/api/provider-vault/catalog`.
- Movies, series, and live TV surfaces show English UI.
- Apollo and XtremeHD sample live channels are visible.
- No legacy `/api/v2/*` endpoint is required during provider-vault mode.
- Stream URL builder uses `/api/provider-vault/stream`.
- No provider credential-shaped URL is rendered or bundled.
- Zero console/page errors.

## VPS deployment

- Deployed static build to `/var/www/davetv/apps/stalker-ui`.
- VPS `index.html` sha256:
  `74f412401d079344d63179364ccb5213085dd6e154f0b4395c37bfdbd1490742`
- Backup:
  `/var/backups/daveai-apps/stalker-ui-before-provider-ready-20260526T1843Z.tar.gz`
- External route:
  `https://apps.daveai.tech/stalker-ui/` returns the expected auth-gate redirect
  to `/login?return_to=...`.
