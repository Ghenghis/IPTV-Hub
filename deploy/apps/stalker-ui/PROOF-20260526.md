# Stalker UI Provider-Vault Proof — 2026-05-26

Status: `provider-ready`

## 2026-05-27 Deep Provider Playback Addendum

Status: `provider-playback verified`

What changed after the first provider-ready pass:

- Apollo Group TV and XtremeHD catalog rows are now interleaved in the hosted
  provider-vault build for live TV, movie detail lookup, and series browsing.
  This keeps both providers visible and playable near the top of the catalog
  instead of hiding XtremeHD behind Apollo's full result set.
- The hosted static build was rebuilt from
  `G:\Github\IPTV-web\stalker-ui` and redeployed to
  `/var/www/davetv/apps/stalker-ui`.
- The hosted Samsung/Tizen `webapis` stub remains served from
  `/webapis/webapis.js`.

Verification:

- `npm run build` passed from `G:\Github\IPTV-web\stalker-ui`.
- Deployed `index.html` sha256:
  `c53ead67954d35c707cda203803e9d3b53f4e90d568bc0a601da98f06f92271c`
- Cloudflare cache was purged for `https://stalker-ui.daveai.tech/` and
  `https://stalker-ui.daveai.tech/index.html`.
- Deep Playwright proof passed for both providers:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\stalker-ui-provider-playback-proof-20260527\summary.json`

Provider proof results:

- Apollo Group TV: `stream200=1`, `segment200=2`, `image200=554`,
  `video.readyState=4`, `videoWidth=1920`, `videoHeight=1080`,
  zero console errors, zero page errors, zero blocking failed requests.
- XtremeHD: `stream200=1`, `segment200=2`, `image200=554`,
  `video.readyState=4`, `videoWidth=1920`, `videoHeight=1080`,
  zero console errors, zero page errors, zero blocking failed requests.

Screenshots:

- `C:\Users\Admin\Downloads\VPS\_visual_artifacts\stalker-ui-provider-playback-proof-20260527\stalker-ui-apollo-player.png`
- `C:\Users\Admin\Downloads\VPS\_visual_artifacts\stalker-ui-provider-playback-proof-20260527\stalker-ui-xtremehd-player.png`

## What changed

- Added a tracked `static-web-overrides/` layer for the live React/Vite
  Stalker UI build used by `apps.daveai.tech/stalker-ui/`.
- Added Apollo Group TV and XtremeHD provider-vault catalog loading for movies,
  series, and live TV.
- Playback URLs now pass through safe same-origin
  `/api/provider-vault/stream` URLs instead of requiring raw provider data.
- Hosted API calls default to same-origin `/api`.
- Live TV now opens on `All Channels` by default so provider channels are
  immediately visible.
- Provider-vault mode returns an empty EPG payload locally instead of hitting
  stale legacy `/api/v2/epg` and producing console errors.
- Added a hosted Samsung/Tizen `webapis` stub and patched the script path to
  `/webapis/webapis.js` so nginx no longer falls back to `index.html` and the
  browser no longer throws `Unexpected token '<'`.

## Verification

- `npm run build` passed from `G:\Github\IPTV-web\stalker-ui`.
- Playwright visual proof passed with stubbed provider-vault endpoints:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\stalker-ui-provider-vault-proof-20260526\summary.json`
- Screenshot:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\stalker-ui-provider-vault-proof-20260526\stalker-ui-provider-vault-live.png`

Assertions from the proof:

- Apollo Group TV and XtremeHD are both configured.
- Provider catalogs load from `/api/provider-vault/catalog`.
- Movies, series, and live TV surfaces show English UI.
- Apollo and XtremeHD sample live channels are visible.
- No legacy `/api/v2/*` endpoint is required during provider-vault mode.
- Stream URL builder uses `/api/provider-vault/stream`.
- No provider credential-shaped URL is rendered or bundled.
- Zero console/page errors.

## VPS deployment

- Deployed static build to `/var/www/davetv/apps/stalker-ui`.
- VPS `index.html` sha256:
  `a4dd4ccb1b4d6bfcc5c8aece15ec04e18a73d9deb3f8f74f9b82013e22b09716`
- Hosted `webapis` stub sha256:
  `b26a491e8b84ff6a53bd84cb9e781f1baccf7cf2662651e3311dca60cc8db922`
- Backup:
  `/var/backups/daveai-apps/stalker-ui-before-provider-ready-20260526T1843Z.tar.gz`
- External route:
  `https://stalker-ui.daveai.tech/` returns the expected auth-gate redirect to
  `/login?return_to=...`.
- Authenticated full-app Playwright sweep passed 12/12:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\apps-provider-ready-sweep-20260526\summary.json`
