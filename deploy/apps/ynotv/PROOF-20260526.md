# ynotv Provider-Vault Proof — 2026-05-27

Status: `provider-ready`

## What Changed

- Added a tracked `static-web-overrides/` layer for the hosted React/Vite build.
- Added `ProviderVaultWebMode` so hosted `ynotv` loads Apollo Group TV and
  XtremeHD from the DaveTV provider vault.
- Live channels and movies use safe same-origin `/api/provider-vault/stream`
  URLs.
- Interleaved Apollo Group TV and XtremeHD rows so both providers are visible
  and playable at the top of the catalog.
- Clicking the already-active card now retries `video.play()`, which recovers
  browsers that buffered the auto-selected first channel but blocked autoplay.
- The hosted build skips native Tauri/mpv source setup and renders English
  DaveTV player chrome.
- HLS playback uses larger buffers:
  `maxBufferLength=180`, `maxMaxBufferLength=600`, `backBufferLength=90`.
- `VITE_DAVETV_HOSTED=1` removes desktop Xtream URL-building chunks from the
  deployed artifact.

## Verification

- `VITE_DAVETV_HOSTED=1 pnpm --filter @ynotv/ui exec vite build` passed from
  `G:\Github\IPTV-web\ynotv`.
- Playwright visual/provider proof passed:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\ynotv-provider-playback-proof-20260527\summary.json`
- Screenshots:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\ynotv-provider-playback-proof-20260527\ynotv-apollo-player.png`
  and
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\ynotv-provider-playback-proof-20260527\ynotv-xtremehd-player.png`

Assertions from the proof:

- Apollo Group TV and XtremeHD are both configured.
- Provider catalogs load from `/api/provider-vault/catalog`.
- Apollo Group TV first visible row is `USA AMC`.
- XtremeHD first visible row is `USA AMC`.
- Both providers reached `video.readyState=4`.
- Both providers reported `1920x1080` playback.
- Both providers advanced `currentTime` after click.
- Stream URLs use same-origin `/api/provider-vault/stream` plus segment proxy
  URLs with HTTP `200`.
- English UI chrome is visible.
- No raw credential-shaped URL is rendered or requested.
- HLS buffer settings are bundled.
- Zero Tauri invoke errors, console errors, page errors, or blocking failed
  requests.

## VPS Deployment

- Deployed static build to `/var/www/davetv/apps/ynotv`.
- VPS `index.html` sha256:
  `1cff4bc043d8137af9057bb05d3cac3ba645b59bb690a29dcb63db3019164ef8`
- Backups:
  `/var/backups/daveai-apps/ynotv-before-provider-interleave-20260527T015700Z.tgz`
  and
  `/var/backups/daveai-apps/ynotv-before-active-click-play-20260527T020400Z.tgz`
- Cloudflare cache was purged for:
  `https://ynotv.daveai.tech/` and
  `https://ynotv.daveai.tech/index.html`.
