# ynotv Provider-Vault Proof — 2026-05-26

Status: `provider-ready`

## What changed

- Added a tracked `static-web-overrides/` layer for the hosted React/Vite build.
- Added `ProviderVaultWebMode` so hosted `ynotv` loads Apollo Group TV and
  XtremeHD from the DaveTV provider vault.
- Live channels and movies use safe same-origin `/api/provider-vault/stream`
  URLs.
- The hosted build skips native Tauri/mpv source setup and renders English
  DaveTV player chrome.
- HLS playback uses larger buffers:
  `maxBufferLength=180`, `maxMaxBufferLength=600`, `backBufferLength=90`.
- `VITE_DAVETV_HOSTED=1` removes desktop Xtream URL-building chunks from the
  deployed artifact.

## Verification

- `VITE_DAVETV_HOSTED=1 pnpm --filter @ynotv/ui exec vite build` passed from
  `G:\Github\IPTV-web\ynotv`.
- Playwright visual/provider proof passed:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\ynotv-provider-vault-proof-20260526\summary.json`
- Screenshot:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\ynotv-provider-vault-proof-20260526\ynotv-provider-vault-live.png`

Assertions from the proof:

- Apollo Group TV and XtremeHD are both configured.
- Provider catalogs load from `/api/provider-vault/catalog`.
- English UI chrome is visible.
- Apollo and XtremeHD sample channels/movies are visible.
- Stream URLs use `/api/provider-vault/stream`.
- No raw credential-shaped URL is rendered or requested.
- HLS buffer settings are bundled.
- Zero Tauri invoke errors, console errors, or page errors.

## VPS deployment

- Deployed static build to `/var/www/davetv/apps/ynotv`.
- VPS `index.html` sha256:
  `f41aade4731c05cce2f72befa388b2917f318049b468fc6e06723b55021e53bf`
- Latest backup:
  `/var/backups/daveai-apps/ynotv-before-hosted-clean-20260526T192034Z.tar.gz`
- Launcher state:
  `provider-ready`, `Apollo + XtremeHD via DaveTV provider vault`.
- External route:
  `https://ynotv.daveai.tech/` returns the expected auth-gate redirect to
  `/login?return_to=...`.
- Deployed asset grep:
  `provider_vault_refs=1`, `large_buffer_refs=1`, `max_buffer_refs=1`,
  `tauri_guard_refs=0`, `raw_credential_leaks=0`.
- Authenticated full-app Playwright sweep passed 12/12:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\apps-provider-ready-sweep-20260526\summary.json`
