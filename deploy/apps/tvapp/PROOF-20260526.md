# TVapp Provider-Vault Proof — 2026-05-27

Status: `provider-ready`

## What Was Verified

- Hosted app: `https://tvapp.daveai.tech/`
- UI is English and provider-vault branded.
- Apollo Group TV and XtremeHD catalogs load from DaveTV provider-vault.
- Playback uses same-origin `/api/provider-vault/stream` URLs with non-empty
  provider ids.
- The player keeps upstream TVapp separate from raw provider credentials; no
  provider host/user/password is rendered in the browser.
- HLS buffering remains tuned for couch playback:
  - live: `maxBufferLength=120`, `maxMaxBufferLength=300`,
    `backBufferLength=90`;
  - VOD: `maxBufferLength=300`, `maxMaxBufferLength=900`,
    `backBufferLength=180`;
  - `lowLatencyMode=false` for steadier buffering.

## Playwright Proof

- Script:
  `scripts/tvapp-provider-playback-proof.mjs`
- Summary:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\tvapp-provider-playback-proof-20260527\summary.json`
- Screenshots:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\tvapp-provider-playback-proof-20260527\tvapp-apollo-player.png`
  and
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\tvapp-provider-playback-proof-20260527\tvapp-xtremehd-player.png`

Assertions from the live proof:

- Apollo Group TV played `USA AMC`.
- XtremeHD played `USA AMC`.
- Both providers reached `video.readyState=4`.
- Both providers reported `1920x1080` playback.
- Both providers used same-origin `/api/provider-vault/stream` requests with
  non-empty `id` query parameters and HTTP `200` responses.
- `0` page errors.
- `0` console errors.
- `0` blocking failed requests.
- One `/api/provider-vault/segment` `net::ERR_ABORTED` was ignored because it
  is the expected hls.js cancellation when the test intentionally switches from
  Apollo playback to XtremeHD playback.

## Sweep Status

The 13-app launcher sweep already passed after the IPTV Restream repair at
`2026-05-27T01:08:16.007Z`. TVapp was then deep-verified with the proof above;
no app code change was required.
