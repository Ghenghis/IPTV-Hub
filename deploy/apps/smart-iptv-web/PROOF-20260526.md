# Smart IPTV Web Provider Playback Proof - 2026-05-26

Status: PASS

Live URL:

```text
https://smart-iptv-web.daveai.tech
```

Artifact directory:

```text
C:\Users\Admin\Downloads\VPS\_visual_artifacts\smart-iptv-web-provider-proof-20260526
```

## Changes

- Deployed the Smart IPTV Web player with real provider-vault playback checks for Apollo Group TV and XtremeHD.
- Made the existing buffering controls real:
  - default HLS buffer target: 300 seconds;
  - HLS max buffer size: 256 MB by default, user-adjustable up to 768 MB;
  - HLS low-latency mode disabled for smoother long-buffer playback;
  - MPEG-TS stash buffer enabled and tied to the same buffer settings;
  - live latency chasing disabled by default so playback favors stability over edge latency.
- Preserved server-side provider-vault behavior: browser storage keeps only `providerId`, not raw host, username, or password.

Tracked override copies:

```text
deploy/apps/smart-iptv-web/overrides/components/player/VideoPlayer.tsx
deploy/apps/smart-iptv-web/overrides/components/dashboard/SettingsView.tsx
```

VPS backup before deployment:

```text
/opt/davetv/backups/20260526T230445Z-smart-buffer
```

## Verification

Command:

```powershell
$env:NODE_PATH='C:\Users\Admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\node_modules'
& 'C:\Users\Admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' scripts\smart-iptv-web-provider-playback-proof.mjs
```

Result:

- Apollo Group TV: PASS
  - catalog: `1200` live, `500` movies, `500` series;
  - selected `USA AMC`;
  - `/api/provider-vault/stream`: `200`;
  - segment proxy: multiple `200`;
  - video `readyState=4`;
  - page errors: `0`;
  - console errors: `0`;
  - browser credential-shaped URL leaks: `0`.
- XtremeHD: PASS
  - catalog: `1200` live, `500` movies, `500` series;
  - selected `USA AMC`;
  - `/api/provider-vault/stream`: `200`;
  - segment proxy: multiple `200`;
  - video `readyState=4`;
  - page errors: `0`;
  - console errors: `0`;
  - browser credential-shaped URL leaks: `0`.

Screenshots:

```text
smart-apollo-dashboard.png
smart-apollo-playback.png
smart-xtremehd-dashboard.png
smart-xtremehd-playback.png
```

## Regression Sweep

After deployment, the full apps launcher sweep was rerun:

```text
C:\Users\Admin\Downloads\VPS\_visual_artifacts\apps-provider-ready-sweep-20260526\summary.json
```

Result: `13/13 passed`.

No login-page regressions, no client-side exception/application-error screens,
no provider credential-shaped text leaks, no Playwright page errors, and no
captured console errors.
