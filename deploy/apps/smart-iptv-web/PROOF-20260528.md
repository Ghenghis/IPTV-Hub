# Smart IPTV Web Strict Provider Proof - 2026-05-28

Status: PASS

Live URL:

```text
https://smart-iptv-web.daveai.tech
```

Artifact directory:

```text
C:\Users\Admin\Downloads\VPS\_visual_artifacts\smart-iptv-web-strict-20260528T152122
```

## Changes

- Fixed provider-vault HLS playback detection. The route `/api/provider-vault/aac-hls` is now always handled by HLS.js, even when the upstream source extension is `ts`.
- Added provider selection modes:
  - Apollo Group TV only;
  - XtremeHD only;
  - Combined Tagged, with both providers loaded and listing groups tagged by provider.
- Preserved credential safety: browser storage contains `providerId` / provider labels only, not raw host, username, or password.
- Kept English catalog profile enabled for provider-vault imports.
- Raised hosted buffer settings options to include 256 MB and 512 MB presets.

Tracked hosted override copies:

```text
deploy/apps/smart-iptv-web/overrides/app/SmartHomeClient.tsx
deploy/apps/smart-iptv-web/overrides/components/auth/Login.tsx
deploy/apps/smart-iptv-web/overrides/components/dashboard/ChannelGrid.tsx
deploy/apps/smart-iptv-web/overrides/components/dashboard/SettingsView.tsx
deploy/apps/smart-iptv-web/overrides/components/player/VideoPlayer.tsx
```

VPS deployment bundle:

```text
/tmp/smart-iptv-web-20260528T151606-smart-hls-detect.tgz
```

VPS backup before deployment:

```text
/opt/davetv/backups/smart-iptv-web-before-20260528T151606-smart-hls-detect.tgz
```

## Verification

Command:

```powershell
$env:NODE_PATH='C:\Users\Admin\node_modules'
$env:SMART_IPTV_WEB_URL='https://smart-iptv-web.daveai.tech'
$env:OUT_DIR='C:\Users\Admin\Downloads\VPS\_visual_artifacts\smart-iptv-web-strict-20260528T152122'
node scripts\smart-iptv-web-provider-playback-proof.mjs apollo xtremehd combined
```

Result: PASS for all requested modes.

| Mode | Catalog | Cards | Playback |
| --- | --- | --- | --- |
| Apollo Group TV | `1200` live, `800` movies, `800` series | Movies and series artwork loaded; live rows rendered with fallback initials where provider logos are absent | selected `|US| NBC 9 HD [ABILENE]`, readyState `4`, 1280x720, unmuted, volume `1`, no media error |
| XtremeHD | `1200` live, `800` movies, `800` series | Live/movie/series cards populated with text and artwork | selected `USA AMC`, readyState `4`, 1920x1080, unmuted, volume `1`, no media error |
| Combined Tagged | Apollo + XtremeHD loaded together | `3978` visible cards sampled, provider tags present in listing text | mode proof verified combined session shape and no browser-side credential leak |

Diagnostic checks:

- Page errors: `0`.
- Console errors: `0`.
- Bad network responses: `0`.
- Browser credential-shaped URL leaks: `0`.
- Provider-vault media responses: HTTP `200`.
- `mpegts.js` no longer receives HLS manifests from `/api/provider-vault/aac-hls`.

Screenshots captured:

```text
smart-apollo-dashboard.png
smart-apollo-live-cards.png
smart-apollo-movies-cards.png
smart-apollo-series-cards.png
smart-xtremehd-dashboard.png
smart-xtremehd-live-cards.png
smart-xtremehd-movies-cards.png
smart-xtremehd-series-cards.png
smart-combined-dashboard.png
smart-combined-live-cards.png
```
