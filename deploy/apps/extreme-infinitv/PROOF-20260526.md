# Extreme InfiniTV Provider-Vault Proof — 2026-05-27

Status: `provider-ready`

## What Changed

- Rebuilt the hosted static bundle with the current upstream-required
  `node:22-alpine` builder. The prior `node:20-alpine` builder no longer
  passes because Astro now requires Node `>=22.12.0`.
- Fixed live playback for DaveTV vault streams. The Live TV page now treats
  root-relative `/api/provider-vault/stream?...` URLs as browser-playable
  same-origin HLS, instead of rejecting them as external/custom-protocol URLs.
- Preserved the provider-vault live-channel order. Apollo Group TV and XtremeHD
  now surface the known-good `USA AMC` rows first instead of alphabetically
  moving flaky `24/7...` entries to the top.

## VPS Deployment

- Live static target:
  `/var/www/davetv/apps/extreme-infinitv`
- Backups:
  `/var/backups/daveai-apps/extreme-infinitv-before-live-playback-fix-20260527T012200Z.tgz`
  and
  `/var/backups/daveai-apps/extreme-infinitv-before-provider-order-fix-20260527T012700Z.tgz`
- Live `index.html` sha256 after the final deploy:
  `85b9cc6f0588ef7252642ba8394d9383da8f8cac7d02eed39645ed4bd65b7306`
- Cloudflare cache was purged for:
  `https://extreme-infinitv.daveai.tech/`,
  `https://extreme-infinitv.daveai.tech/livetv`,
  and `https://extreme-infinitv.daveai.tech/livetv/`.

## Playwright Proof

- Script:
  `scripts/extreme-infinitv-provider-playback-proof.mjs`
- Summary:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\extreme-infinitv-provider-playback-proof-20260527\summary.json`
- Screenshots:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\extreme-infinitv-provider-playback-proof-20260527\extreme-infinitv-apollo-player.png`
  and
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\extreme-infinitv-provider-playback-proof-20260527\extreme-infinitv-xtremehd-player.png`

Assertions from the live proof:

- Apollo Group TV first row is `USA AMC`.
- XtremeHD first row is `USA AMC`.
- Both providers reached `video.readyState=4`.
- Both providers reported `1920x1080` playback.
- Both providers used same-origin `/api/provider-vault/stream` requests with
  non-empty `id` query parameters and HTTP `200` responses.
- Provider HLS segments came through same-origin `/api/provider-vault/segment`
  with HTTP `200` responses.
- UI text is English.
- No `embedded player. Set up MPV` unsupported-scheme error remains.
- `0` page errors.
- `0` console errors.
- `0` blocking failed requests.

## Sweep Status

The 13-app launcher sweep passed again after this deploy at
`2026-05-27T01:31:08.142Z` with `13/13` apps passing, `0` login regressions,
`0` client-side exception/application-error screens, `0` provider credential
text leaks, `0` page errors, and `0` captured console errors.
