# IPTV Stream Provider-Vault Proof — 2026-05-27

Status: superseded by stricter proof

Superseded by:
`docs/IPTV_STREAM_STRICT_PROVIDER_ACCEPTED_20260529.md`

The newer proof uses the English provider-vault profile and requires unmuted
playback with decoded audio bytes. Keep this file only as historical context
for the original provider-vault bring-up.

## What Was Verified

- Hosted app: `https://iptv-stream.daveai.tech/`
- UI is English (`StreamOS`, `Live`, `All Channels`, provider status copy).
- Apollo Group TV and XtremeHD catalogs load from the DaveTV provider vault.
- Playback uses same-origin `/api/provider-vault/aac-hls` URLs with non-empty
  provider item ids and sanitized segment-file parameters.
- The browser only stores DaveTV provider ids and vault catalog URLs, not raw
  provider host/user/password values.
- The proof uses a fresh browser context per provider so provider-switch aborts
  cannot be mistaken for app errors.

## Playwright Proof

- Script:
  `scripts/iptv-stream-provider-playback-proof.mjs`
- Summary:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\iptv-stream-provider-playback-proof-20260527\summary.json`
- Screenshots:
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\iptv-stream-provider-playback-proof-20260527\iptv-stream-apollo-player.png`
  and
  `C:\Users\Admin\Downloads\VPS\_visual_artifacts\iptv-stream-provider-playback-proof-20260527\iptv-stream-xtremehd-player.png`

Assertions from the live proof:

- Apollo Group TV first row is `USA AMC`.
- XtremeHD first row is `USA AMC`.
- Both providers reached `video.readyState=4`.
- Both providers reported `1920x1080` playback.
- Both providers played through same-origin `/api/provider-vault/aac-hls`
  manifest and segment responses with HTTP `200`.
- UI text is English.
- No raw provider credential-shaped text rendered in the browser.
- `0` page errors.
- `0` console errors.
- `0` blocking failed requests.

## Sweep Status

The 13-app launcher sweep passed immediately before this proof at
`2026-05-27T01:31:08.142Z`. No app code change or redeploy was required for
IPTV Stream; this pass adds the dedicated provider-playback evidence.
